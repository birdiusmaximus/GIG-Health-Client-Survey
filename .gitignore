.DS_Store
node_modules/
.vercel
*.log

# Local env files — secrets must never be committed
.env
.env.local
.env.*.local

# Build output (if any tooling adds it later)
dist/
build/

# Local working folder for video / image generation sources.
# Render outputs that ship to the site live in /assets/videos/.
Animations/

# Local-only backup of the original 4K renders. The shipped videos
# in /assets/videos/ are 1080p re-encodes; the 4K originals stay on
# disk as a safety net but are too large to commit.
assets/videos/original-4k/
